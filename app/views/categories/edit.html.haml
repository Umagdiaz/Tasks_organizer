.category.edit-page
    .card
        .card-body
            %h2 Editing category

            = render 'form'

            = link_to 'Show', @category, class: "btn btn-outline-info"
            \|
            = link_to 'Back', categories_path, class: "btn btn-outline-secondary"
